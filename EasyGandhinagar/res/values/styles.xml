<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="MyApp.PopupMenu" parent="android:Widget.Holo.Light.ListPopupWindow">
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="btnStylelBluelight" parent="@android:style/Widget.Button"><item name="android:textSize">@dimen/TextSize_mediam</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/btn_lightblue</item>
        <item name="android:padding">10dip</item>
    </style>
    
    <style name="btnStylelBluelight_pub" parent="@android:style/Widget.Button"><item name="android:textSize">@dimen/TextSize_mediam</item>
        <item name="android:textColor">#000000</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/btn_lightblue_pub</item>
        <item name="android:padding">10dip</item>
    </style>
</resources>